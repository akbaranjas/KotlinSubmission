<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
            android:id="@+id/match_menu"
            android:enabled="true"
            android:title="@string/str_match"
            android:icon="@drawable/ic_football_match_on_tv"
            app:showAsAction="ifRoom"/>

    <item
            android:id="@+id/teams_menu"
            android:enabled="true"
            android:icon="@drawable/ic_teams"
            android:title="@string/str_teams"
            app:showAsAction="ifRoom" />

    <item
            android:id="@+id/favourites_menu"
            android:enabled="true"
            android:icon="@drawable/ic_favourites"
            android:title="@string/str_favorites"
            app:showAsAction="ifRoom" />
</menu>